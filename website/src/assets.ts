import markUrl from '../../extension/public/icons/mark.svg?url';

/** Assets shared directly with the extension so the two surfaces cannot drift. */
export const brandAssets = {
  markUrl,
} as const;
